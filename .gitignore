node_modules/
dist/
# Catalog JSON is regenerated from the authored records on every build.
src/data/generated/*
# The image manifest is not: it records which file, which licence and which
# author, and the binaries are re-fetchable from it. It is committed.
!src/data/generated/images.json
*.local
.DS_Store

# Reviewed vehicle photography is committed with its manifest so a clean
# checkout and every deployment contain the files the UI references.
tsconfig.tsbuildinfo

# Wrangler local state (dev server caches, generated types).
.wrangler/
